apiVersion: v1
kind: Service
metadata:
    name: sergiotecoacrbuildhelloworldnode
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sergiotecoacrbuildhelloworldnode